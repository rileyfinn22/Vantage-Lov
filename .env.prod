# Database Configuration
DB_USER=postgres
DB_PASSWORD=changeme_strong_password_here
DB_DATABASE=vantage

# Application Configuration
HOST=37.70.209.224
TENANT_NAME=vantage-prod
NODE_ENV=production

# SOPS Age Key (for encrypted secrets)
# Leave empty if not using encrypted secrets
MISE_SOPS_AGE_KEY=

# Optional: Additional trusted origins (comma-separated)
# TRUSTED_ORIGINS=https://example.com,https://another-domain.com
